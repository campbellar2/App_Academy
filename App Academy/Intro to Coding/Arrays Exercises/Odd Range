def odd_range(min, max)
    i = min
    oddNums = []
    while i <= max
        if i % 2 == 0
            i += 1
        end
        oddNums << i
        
        i += 2
    end
    return oddNums
end

print odd_range(11, 18) # => [11, 13, 15, 17]
puts
print odd_range(3, 7)   # => [3, 5, 7]
